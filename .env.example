# Copy this file to .env and fill in your own values.
# The .env file must never be committed to GitHub.

# From Atlas: Connect -> Drivers -> Python. Replace <db_password> with your
# database user's password (angle brackets included).
MONGODB_URI=mongodb+srv://ramanayakedushan_db_user:0P2HLr3nWBo8Nc5g@pet-dbms-cluster.vkox1e8.mongodb.net/?appName=pet-dbms-cluster

DB_NAME=SignUp
COLLECTION_NAME=users
